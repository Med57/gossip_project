<h1>Edition du Gossip</h1>

<div class="row justify-content-center">
  <div class="col-6">
<%= form_for @gossip do |f| %>
  <div class="form-group">
    <label> Titre du gossip </label>
    <%= f.text_field :title %>
  </div>
  <div class="form-group">
    <label> Contenu du gossip </label>
    <%= f.text_area :content %>
  </div>
<div class="form-group">
    <button type="submit" class="btn btn-primary"name="button">Mettre à jour</button>
  </div>
  </div>
<% end %>
</div>
